---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import { GLSLHills } from '../components/ui/GLSLHills';
---

<Layout title="Mi Perfil UX - Jorge Cabrera">
  <Navbar />
  
  <!-- Hero Section: GLSL Animated -->
  <section class="hero-glsl relative min-h-screen flex items-start justify-center overflow-hidden pt-32 bg-black">
    <!-- GLSL Hills Background (bottom positioned) -->
    <div class="absolute inset-0 flex items-end">
      <div class="w-full h-[70vh]">
        <GLSLHills 
          client:load 
          variant="bright-red"
          speed={0.4}
          opacity={0.85}
          cameraZ={100}
        />
      </div>
    </div>
    
    <!-- Content Overlay (top positioned) -->
    <div class="container mx-auto px-6 relative z-10 pt-12">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="chaos-title mb-6">
          <span class="block text-3xl sm:text-4xl md:text-6xl lg:text-7xl font-black text-white leading-tight" style="font-family: 'Syne', sans-serif; text-shadow: 0 4px 20px rgba(0,0,0,0.5);">
            Mi background es <span class="text-[#ff3333]">mi superpoder</span>
          </span>
        </h1>
        <p class="text-base sm:text-lg md:text-xl text-neutral-300 font-['Poppins'] max-w-2xl mx-auto" style="text-shadow: 0 2px 10px rgba(0,0,0,0.7);">
          Autodidacta. Analítico. Solopreneur.
        </p>
      </div>
    </div>
  </section>

  <!-- Section 1: Bento Box Asimétrico - Filosofía -->
  <section class="section-bento py-32 bg-white relative overflow-hidden">
    <div class="container mx-auto px-6">
      <div class="chaos-grid grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
        <!-- Imagen grande -->
        <div class="lg:col-span-7 reveal-on-scroll" data-delay="0">
          <div class="relative rounded-3xl overflow-hidden shadow-2xl" style="transform: rotate(-3deg);">
            <img 
              src="/img/Jorge-Cabrera-Libros.jpg" 
              alt="Filosofía y pensamiento crítico" 
              class="w-full h-[500px] object-cover"
            />
            <div class="absolute top-6 right-6 bg-white/90 backdrop-blur-sm px-4 py-2 rounded-full text-sm font-semibold text-neutral-800">
              Filosofía → UX
            </div>
          </div>
        </div>
        
        <!-- Texto -->
        <div class="lg:col-span-5 reveal-on-scroll" data-delay="200">
          <div class="bg-neutral-50 p-8 rounded-3xl border-4 border-neutral-200" style="transform: rotate(2deg);">
            <h2 class="text-4xl font-black mb-4" style="font-family: 'Space Grotesk', sans-serif; color: #b80000;">
              De preguntas filosóficas<br/>a soluciones de diseño
            </h2>
            <p class="text-lg text-neutral-700 font-['Poppins'] leading-relaxed">
              Años enseñando filosofía me entrenaron en hacer <strong>las preguntas correctas</strong>. 
              Aprendí que entender el "por qué" es el primer paso para diseñar el "cómo".
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 2: Pollitas - La Necesidad como Maestra -->
  <section class="section-zigzag py-32 bg-gradient-to-b from-white to-neutral-100 relative overflow-hidden">
    <div class="container mx-auto px-6">
      <div class="max-w-6xl mx-auto">
        <div class="chaos-grid grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
          <!-- Imagen -->
          <div class="lg:col-span-7 reveal-on-scroll" data-delay="0">
            <div class="relative rounded-3xl overflow-hidden shadow-2xl" style="transform: rotate(3deg);">
              <img 
                src="/img/Aves-gestión.jpg" 
                alt="Gestión avícola - La necesidad como maestra" 
                class="w-full h-[400px] object-cover"
              />
            </div>
          </div>
          
          <!-- Texto -->
          <div class="lg:col-span-5 reveal-on-scroll" data-delay="200">
            <div class="bg-neutral-50 p-8 rounded-3xl border-4 border-neutral-200" style="transform: rotate(-2deg);">
              <h2 class="text-4xl font-black mb-4" style="font-family: 'Space Grotesk', sans-serif; color: #b80000;">
                La necesidad<br/>
                <span class="text-neutral-900">como mejor maestra</span>
              </h2>
              <p class="text-lg text-neutral-700 font-['Poppins'] leading-relaxed">
                Sin conocimientos previos en avicultura, gestión ni código. 
                Solo la capacidad de <strong>aprender rápido y resolver problemas</strong> con lo disponible.
                De la granja avícola al desarrollo fullstack: la adaptabilidad se vive, no se enseña.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 3: Cards Flotantes - Mi Proceso -->
  <section class="section-philosophy relative py-24 md:py-40 bg-gradient-to-br from-neutral-900 via-neutral-800 to-black overflow-hidden">
    <!-- Floating Elements -->
    <div class="floating-element absolute top-10 left-10 w-16 h-16 md:w-24 md:h-24 rounded-full bg-[#ff0000]/20 blur-xl animate-float" style="animation-delay: 0s;"></div>
    <div class="floating-element absolute top-32 right-16 w-20 h-20 md:w-32 md:h-32 rounded-full bg-[#ff3333]/15 blur-2xl animate-float" style="animation-delay: 1s;"></div>
    <div class="floating-element absolute bottom-20 left-1/4 w-12 h-12 md:w-20 md:h-20 rounded-full bg-white/10 blur-xl animate-float" style="animation-delay: 2s;"></div>
    <div class="floating-element absolute bottom-32 right-1/3 w-16 h-16 md:w-28 md:h-28 rounded-full bg-[#b80000]/20 blur-2xl animate-float" style="animation-delay: 1.5s;"></div>
    
    <div class="container mx-auto px-6 relative z-10">
      <div class="max-w-4xl mx-auto text-center">
        <!-- Philosophy Quote -->
        <div class="reveal-on-scroll" data-delay="0">
          <blockquote class="text-3xl md:text-5xl lg:text-6xl font-black mb-8 text-white" style="font-family: 'Space Grotesk', sans-serif; text-shadow: 0 4px 20px rgba(0,0,0,0.5);">
            El diseño es traducir<br/>
            <span class="text-[#ff3333]">problemas humanos</span><br/>
            en soluciones intuitivas
          </blockquote>
          <p class="text-lg md:text-xl text-neutral-300 font-['Poppins'] italic mb-10">
            Cada línea de código, cada pixel, cada decisión tiene un propósito
          </p>
          <div class="flex flex-wrap justify-center gap-3">
            <span class="px-5 py-3 bg-white/10 backdrop-blur-sm rounded-full text-sm font-medium text-white border border-white/20">Usuario primero</span>
            <span class="px-5 py-3 bg-white/10 backdrop-blur-sm rounded-full text-sm font-medium text-white border border-white/20">Data-driven</span>
            <span class="px-5 py-3 bg-white/10 backdrop-blur-sm rounded-full text-sm font-medium text-white border border-white/20">Iterativo</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 4: Cards Flotantes Superpuestas - Metodología -->
  <section class="section-cards py-32 bg-gradient-to-b from-white to-neutral-100 relative overflow-hidden">
    <div class="container mx-auto px-6">
      <div class="max-w-6xl mx-auto">
        <h2 class="text-5xl md:text-6xl font-black text-center mb-20 reveal-on-scroll" style="font-family: 'Syne', sans-serif; color: #111;">
          Mi Proceso
        </h2>
        
        <div class="relative min-h-[600px]">
          <!-- Card 1: Diseñar (ARRIBA) -->
          <div class="float-card absolute top-0 right-0 lg:right-12 w-full lg:w-[400px] reveal-on-scroll" data-delay="0" style="transform: rotate(3deg);">
            <div class="bg-neutral-900 text-white p-8 rounded-3xl shadow-2xl">
              <div class="w-16 h-16 bg-white rounded-2xl flex items-center justify-center mb-4" style="transform: rotate(-8deg);">
                <span class="text-3xl">🎨</span>
              </div>
              <h3 class="text-2xl font-bold mb-3 font-['Space Grotesk']">Diseñar</h3>
              <p class="text-neutral-300 font-['Poppins']">
                Prototipar rápido. Validar con usuarios. Iterar sin miedo al cambio.
              </p>
            </div>
          </div>
          
          <!-- Card 2: Analizar -->
          <div class="float-card absolute top-32 lg:top-20 left-0 lg:left-12 w-full lg:w-[400px] reveal-on-scroll" data-delay="200" style="transform: rotate(-4deg);">
            <div class="bg-white p-8 rounded-3xl shadow-2xl border-4 border-neutral-200">
              <div class="w-16 h-16 bg-[#b80000] rounded-2xl flex items-center justify-center mb-4" style="transform: rotate(12deg);">
                <span class="text-3xl">🔍</span>
              </div>
              <h3 class="text-2xl font-bold mb-3 font-['Space Grotesk']">Analizar</h3>
              <p class="text-neutral-700 font-['Poppins']">
                Investigación profunda. Preguntas incómodas. Datos reales, no suposiciones.
              </p>
            </div>
          </div>
          
          <!-- Card 3: Ejecutar -->
          <div class="float-card absolute top-80 lg:top-56 left-0 right-0 mx-auto w-full lg:w-[400px] reveal-on-scroll" data-delay="400" style="transform: rotate(-2deg);">
            <div class="bg-[#b80000] text-white p-8 rounded-3xl shadow-2xl">
              <div class="w-16 h-16 bg-white rounded-2xl flex items-center justify-center mb-4" style="transform: rotate(6deg);">
                <span class="text-3xl">⚡</span>
              </div>
              <h3 class="text-2xl font-bold mb-3 font-['Space Grotesk']">Ejecutar</h3>
              <p class="text-white/90 font-['Poppins']">
                Código limpio. Sistemas escalables. Soluciones simples a problemas complejos.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contacto + Footer -->
  <section id="contacto" class="py-20 bg-gradient-to-br from-[#2a2a2a] to-[#111111]">
    <div class="container mx-auto px-4 sm:px-6">
      <div class="max-w-4xl mx-auto text-center mb-12">
        <h2 class="text-2xl xs:text-3xl sm:text-4xl md:text-5xl font-black font-['Syne'] mb-6 text-[#FAF9F6] leading-tight">
          🔴 Hablemos
        </h2>
        <p class="text-xl text-[#d1d1d1] mb-12 max-w-2xl mx-auto leading-relaxed font-['Poppins']">
          No solo diseño. Resuelvo problemas. Con la lógica de un gestor y la empatía de un profesor.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="mailto:jcabreralabbe@gmail.com"
            class="px-8 py-4 border-2 border-[#FAF9F6] text-[#FAF9F6] font-semibold rounded-full transition-all hover:bg-[#b80000] hover:border-[#b80000] hover:scale-105 no-underline"
          >
            Email
          </a>
          <a 
            href="https://linkedin.com/in/jorge-cabrera-ux" 
            target="_blank"
            rel="noopener noreferrer"
            class="px-8 py-4 border-2 border-[#FAF9F6] text-[#FAF9F6] font-semibold rounded-full transition-all hover:bg-[#b80000] hover:border-[#b80000] hover:scale-105 no-underline"
          >
            LinkedIn
          </a>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  /* Animaciones de scroll reveal */
  .reveal-on-scroll {
    opacity: 0;
    transform: translateY(50px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  .reveal-on-scroll.revealed {
    opacity: 1;
    transform: translateY(0);
  }

  /* Floating badges animation */
  .floating-badge {
    animation: float 6s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0) rotate(12deg);
    }
    50% {
      transform: translateY(-20px) rotate(12deg);
    }
  }

  .floating-badge:last-child {
    animation-delay: 3s;
  }

  /* Floating elements animation */
  .floating-element {
    animation: animate-float 8s ease-in-out infinite;
  }

  @keyframes animate-float {
    0%, 100% {
      transform: translateY(0) translateX(0) scale(1);
    }
    33% {
      transform: translateY(-30px) translateX(20px) scale(1.1);
    }
    66% {
      transform: translateY(-15px) translateX(-15px) scale(0.95);
    }
  }

  /* Skill badges */
  .skill-badge {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background: linear-gradient(135deg, #f5f5f5 0%, #e5e5e5 100%);
    border: 2px solid #d4d4d4;
    border-radius: 2rem;
    font-family: 'Poppins', sans-serif;
    font-weight: 600;
    color: #333;
    transition: all 0.3s ease;
  }

  .skill-badge:hover {
    background: linear-gradient(135deg, #b80000 0%, #950000 100%);
    color: white;
    border-color: #b80000;
    transform: translateY(-4px) rotate(2deg);
    box-shadow: 0 8px 20px rgba(184, 0, 0, 0.3);
  }

  /* Responsive adjustments */
  @media (max-width: 1024px) {
    .float-card {
      position: relative !important;
      top: auto !important;
      left: auto !important;
      right: auto !important;
      margin-bottom: 2rem;
    }
  }

  @media (max-width: 768px) {
    .chaos-title span {
      font-size: 2.5rem !important;
    }
    
    .floating-badge {
      font-size: 0.875rem;
      padding: 0.5rem 1rem;
    }
  }
</style>

<script>
  // Scroll reveal animation
  document.addEventListener('DOMContentLoaded', () => {
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -100px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const delay = entry.target.getAttribute('data-delay') || '0';
          setTimeout(() => {
            entry.target.classList.add('revealed');
          }, parseInt(delay));
          observer.unobserve(entry.target);
        }
      });
    }, observerOptions);

    document.querySelectorAll('.reveal-on-scroll').forEach(el => {
      observer.observe(el);
    });

    // Timeline items animation
    const timelineItems = document.querySelectorAll('.timeline-item');
    
    const timelineObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          timelineObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });
    
    timelineItems.forEach(item => {
      timelineObserver.observe(item);
    });
  });
</script>

