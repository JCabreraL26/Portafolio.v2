---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import { GLSLHills } from '../components/ui/GLSLHills';
import RotatingEarth from '../components/ui/wireframe-dotted-globe';
---

<Layout title="Mi Perfil UX - Jorge Cabrera">
  <Navbar />
  
  <!-- Hero Section: GLSL Animated -->
  <section class="hero-glsl relative min-h-screen flex items-start justify-center overflow-hidden pt-32 bg-black">
    <!-- GLSL Hills Background (bottom positioned) -->
    <div class="absolute inset-0 flex items-end">
      <div class="w-full h-[70vh]">
        <GLSLHills 
          client:load 
          variant="bright-red"
          speed={0.4}
          opacity={0.85}
          cameraZ={100}
        />
      </div>
    </div>
    
    <!-- Content Overlay (top positioned) -->
    <div class="container mx-auto px-6 relative z-10 pt-12">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="chaos-title mb-8">
          <span class="block text-6xl md:text-8xl font-black mb-4 text-white" style="font-family: 'Space Grotesk', sans-serif; text-shadow: 0 4px 20px rgba(0,0,0,0.5);">
            Resolviendo
          </span>
          <span class="block text-5xl md:text-7xl font-bold text-[#ff3333]" style="font-family: 'Syne', sans-serif; text-shadow: 0 4px 20px rgba(255,0,0,0.3);">
            Problemas
          </span>
          <span class="block text-4xl md:text-6xl font-light mt-2 text-neutral-200" style="font-family: 'Poppins', sans-serif; text-shadow: 0 2px 10px rgba(0,0,0,0.5);">
            Diseñando Soluciones
          </span>
        </h1>
        <p class="text-xl md:text-2xl text-neutral-300 font-['Poppins'] max-w-2xl mx-auto" style="text-shadow: 0 2px 10px rgba(0,0,0,0.7);">
          Autodidacta. Gestor. Solucionador de problemas.
        </p>
      </div>
    </div>
    
    <!-- Floating badges -->
    <div class="floating-badge absolute top-40 right-8 md:right-12 bg-[#ff0000] text-white px-4 md:px-6 py-2 md:py-3 rounded-full font-bold shadow-2xl" style="transform: rotate(12deg); text-shadow: 0 2px 8px rgba(0,0,0,0.5);">
      UX Designer
    </div>
    <div class="floating-badge absolute top-56 left-8 md:left-12 bg-white text-neutral-900 px-4 md:px-6 py-2 md:py-3 rounded-full font-bold shadow-2xl" style="transform: rotate(-8deg);">
      Full Stack Dev
    </div>
  </section>

  <!-- Section 1: Bento Box Asimétrico - Filosofía -->
  <section class="section-bento py-32 bg-white relative overflow-hidden">
    <div class="container mx-auto px-6">
      <div class="chaos-grid grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
        <!-- Imagen grande -->
        <div class="lg:col-span-7 reveal-on-scroll" data-delay="0">
          <div class="relative rounded-3xl overflow-hidden shadow-2xl" style="transform: rotate(-3deg);">
            <img 
              src="/img/Jorge-Cabrera-Libros.jpg" 
              alt="Filosofía y pensamiento crítico" 
              class="w-full h-[500px] object-cover"
            />
            <div class="absolute top-6 right-6 bg-white/90 backdrop-blur-sm px-4 py-2 rounded-full text-sm font-semibold text-neutral-800">
              Filosofía → UX
            </div>
          </div>
        </div>
        
        <!-- Texto -->
        <div class="lg:col-span-5 reveal-on-scroll" data-delay="200">
          <div class="bg-neutral-50 p-8 rounded-3xl border-4 border-neutral-200" style="transform: rotate(2deg);">
            <h2 class="text-4xl font-black mb-4" style="font-family: 'Space Grotesk', sans-serif; color: #b80000;">
              De preguntas filosóficas<br/>a soluciones de diseño
            </h2>
            <p class="text-lg text-neutral-700 font-['Poppins'] leading-relaxed">
              Años enseñando filosofía me entrenaron en hacer <strong>las preguntas correctas</strong>. 
              Aprendí que entender el "por qué" es el primer paso para diseñar el "cómo".
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 2: Zigzag Overlap - Autodidacta -->
  <section class="section-zigzag py-32 bg-gradient-to-b from-white to-neutral-100 relative overflow-hidden">
    <div class="container mx-auto px-6">
      <div class="max-w-6xl mx-auto">
        <!-- Texto arriba derecha -->
        <div class="ml-auto max-w-xl reveal-on-scroll mb-12" data-delay="0" style="transform: translateX(40px);">
          <h2 class="text-5xl font-black mb-6" style="font-family: 'Syne', sans-serif; color: #111;">
            La necesidad<br/>
            <span class="text-[#b80000]">como mejor maestra</span>
          </h2>
          <p class="text-lg text-neutral-700 font-['Poppins'] leading-relaxed">
            Sin conocimientos previos en avicultura, gestión ni código. 
            Solo la capacidad de <strong>aprender rápido y resolver problemas</strong> con lo disponible.
          </p>
        </div>
        
        <!-- Imagen pequeña izquierda -->
        <div class="max-w-md reveal-on-scroll" data-delay="200" style="transform: translateX(-40px);">
          <div class="relative rounded-2xl overflow-hidden shadow-xl" style="transform: rotate(3deg);">
            <img 
              src="/img/Aves-gestión.jpg" 
              alt="Sistemas y gestión" 
              class="w-full h-[400px] object-cover"
            />
          </div>
        </div>
        
        <!-- Quote destacado -->
        <div class="max-w-2xl mx-auto mt-12 reveal-on-scroll" data-delay="400">
          <blockquote class="bg-[#b80000] text-white p-10 rounded-3xl text-center shadow-2xl" style="transform: rotate(-1deg);">
            <p class="text-2xl md:text-3xl font-bold font-['Poppins'] italic">
              "De granja avícola a código: La adaptabilidad no se enseña, se vive"
            </p>
          </blockquote>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 3: Philosophy Quote + 3D Globe -->
  <section class="section-philosophy relative py-20 md:py-32 bg-white overflow-hidden">
    <div class="container mx-auto px-6">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-center max-w-6xl mx-auto">
        <!-- 3D Globe (Left) -->
        <div class="reveal-on-scroll order-2 md:order-1" data-delay="0">
          <RotatingEarth 
            client:load
            width={600}
            height={600}
            className="w-full"
          />
        </div>
        
        <!-- Philosophy Text (Right) -->
        <div class="reveal-on-scroll order-1 md:order-2" data-delay="200">
          <blockquote class="text-3xl md:text-5xl lg:text-6xl font-black mb-6" style="font-family: 'Space Grotesk', sans-serif; color: #111;">
            El diseño es traducir<br/>
            <span class="text-[#b80000]">problemas humanos</span><br/>
            en soluciones intuitivas
          </blockquote>
          <p class="text-lg md:text-xl text-neutral-600 font-['Poppins'] italic">
            Cada línea de código, cada pixel, cada decisión tiene un propósito
          </p>
          <div class="mt-8 flex flex-wrap gap-3">
            <span class="px-4 py-2 bg-neutral-100 rounded-full text-sm font-medium text-neutral-700">Usuario primero</span>
            <span class="px-4 py-2 bg-neutral-100 rounded-full text-sm font-medium text-neutral-700">Data-driven</span>
            <span class="px-4 py-2 bg-neutral-100 rounded-full text-sm font-medium text-neutral-700">Iterativo</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 4: Cards Flotantes Superpuestas - Metodología -->
  <section class="section-cards py-32 bg-gradient-to-b from-white to-neutral-100 relative overflow-hidden">
    <div class="container mx-auto px-6">
      <div class="max-w-6xl mx-auto">
        <h2 class="text-5xl md:text-6xl font-black text-center mb-20 reveal-on-scroll" style="font-family: 'Syne', sans-serif; color: #111;">
          Mi Proceso
        </h2>
        
        <div class="relative min-h-[600px]">
          <!-- Card 1: Diseñar (ARRIBA) -->
          <div class="float-card absolute top-0 right-0 lg:right-12 w-full lg:w-[400px] reveal-on-scroll" data-delay="0" style="transform: rotate(3deg);">
            <div class="bg-neutral-900 text-white p-8 rounded-3xl shadow-2xl">
              <div class="w-16 h-16 bg-white rounded-2xl flex items-center justify-center mb-4" style="transform: rotate(-8deg);">
                <span class="text-3xl">🎨</span>
              </div>
              <h3 class="text-2xl font-bold mb-3 font-['Space Grotesk']">Diseñar</h3>
              <p class="text-neutral-300 font-['Poppins']">
                Prototipar rápido. Validar con usuarios. Iterar sin miedo al cambio.
              </p>
            </div>
          </div>
          
          <!-- Card 2: Analizar -->
          <div class="float-card absolute top-32 lg:top-20 left-0 lg:left-12 w-full lg:w-[400px] reveal-on-scroll" data-delay="200" style="transform: rotate(-4deg);">
            <div class="bg-white p-8 rounded-3xl shadow-2xl border-4 border-neutral-200">
              <div class="w-16 h-16 bg-[#b80000] rounded-2xl flex items-center justify-center mb-4" style="transform: rotate(12deg);">
                <span class="text-3xl">🔍</span>
              </div>
              <h3 class="text-2xl font-bold mb-3 font-['Space Grotesk']">Analizar</h3>
              <p class="text-neutral-700 font-['Poppins']">
                Investigación profunda. Preguntas incómodas. Datos reales, no suposiciones.
              </p>
            </div>
          </div>
          
          <!-- Card 3: Ejecutar -->
          <div class="float-card absolute top-80 lg:top-56 left-0 right-0 mx-auto w-full lg:w-[400px] reveal-on-scroll" data-delay="400" style="transform: rotate(-2deg);">
            <div class="bg-[#b80000] text-white p-8 rounded-3xl shadow-2xl">
              <div class="w-16 h-16 bg-white rounded-2xl flex items-center justify-center mb-4" style="transform: rotate(6deg);">
                <span class="text-3xl">⚡</span>
              </div>
              <h3 class="text-2xl font-bold mb-3 font-['Space Grotesk']">Ejecutar</h3>
              <p class="text-white/90 font-['Poppins']">
                Código limpio. Sistemas escalables. Soluciones simples a problemas complejos.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Skills Highlights -->
  <section class="section-skills py-20 bg-white">
    <div class="container mx-auto px-6">
      <div class="max-w-4xl mx-auto">
        <div class="flex flex-wrap gap-4 justify-center reveal-on-scroll">
          <span class="skill-badge">UX Research</span>
          <span class="skill-badge">Prototyping</span>
          <span class="skill-badge">Full Stack Dev</span>
          <span class="skill-badge">Systems Thinking</span>
          <span class="skill-badge">IA Tools</span>
          <span class="skill-badge">Design Thinking</span>
          <span class="skill-badge">Gestión de Proyectos</span>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section: Split Diagonal -->
  <section class="section-cta py-32 bg-gradient-to-br from-neutral-900 to-neutral-800 relative overflow-hidden">
    <div class="absolute inset-0 opacity-10" style="background-image: repeating-linear-gradient(-45deg, #b80000 0%, #b80000 1px, transparent 1px, transparent 12px);"></div>
    
    <div class="container mx-auto px-6 relative z-10">
      <div class="max-w-6xl mx-auto">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
          <!-- Imagen -->
          <div class="reveal-on-scroll" data-delay="0">
            <div class="relative rounded-3xl overflow-hidden" style="transform: rotate(-2deg);">
              <img 
                src="/img/caos_to_order.jpg" 
                alt="Proyectos realizados" 
                class="w-full h-[400px] object-cover"
              />
            </div>
          </div>
          
          <!-- Botones -->
          <div class="text-center lg:text-left reveal-on-scroll" data-delay="200">
            <h2 class="text-4xl md:text-5xl font-black text-white mb-6" style="font-family: 'Syne', sans-serif;">
              ¿Listo para colaborar?
            </h2>
            <p class="text-xl text-neutral-300 mb-8 font-['Poppins']">
              Problemas complejos merecen soluciones creativas
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
              <a 
                href="/proyectos/localshop-pro" 
                class="cta-button bg-[#b80000] hover:bg-[#950000] text-white px-8 py-4 rounded-full font-bold text-lg transition-all shadow-xl hover:shadow-2xl hover:scale-105"
              >
                Ver Proyectos
              </a>
              <a 
                href="/#contacto" 
                class="cta-button bg-white hover:bg-neutral-100 text-neutral-900 px-8 py-4 rounded-full font-bold text-lg transition-all shadow-xl hover:shadow-2xl hover:scale-105"
              >
                Contactar
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  /* Animaciones de scroll reveal */
  .reveal-on-scroll {
    opacity: 0;
    transform: translateY(50px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  .reveal-on-scroll.revealed {
    opacity: 1;
    transform: translateY(0);
  }

  /* Floating badges animation */
  .floating-badge {
    animation: float 6s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0) rotate(12deg);
    }
    50% {
      transform: translateY(-20px) rotate(12deg);
    }
  }

  .floating-badge:last-child {
    animation-delay: 3s;
  }

  /* Skill badges */
  .skill-badge {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background: linear-gradient(135deg, #f5f5f5 0%, #e5e5e5 100%);
    border: 2px solid #d4d4d4;
    border-radius: 2rem;
    font-family: 'Poppins', sans-serif;
    font-weight: 600;
    color: #333;
    transition: all 0.3s ease;
  }

  .skill-badge:hover {
    background: linear-gradient(135deg, #b80000 0%, #950000 100%);
    color: white;
    border-color: #b80000;
    transform: translateY(-4px) rotate(2deg);
    box-shadow: 0 8px 20px rgba(184, 0, 0, 0.3);
  }

  /* Responsive adjustments */
  @media (max-width: 1024px) {
    .float-card {
      position: relative !important;
      top: auto !important;
      left: auto !important;
      right: auto !important;
      margin-bottom: 2rem;
    }
  }

  @media (max-width: 768px) {
    .chaos-title span {
      font-size: 2.5rem !important;
    }
    
    .floating-badge {
      font-size: 0.875rem;
      padding: 0.5rem 1rem;
    }
  }
</style>

<script>
  // Scroll reveal animation
  document.addEventListener('DOMContentLoaded', () => {
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -100px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const delay = entry.target.getAttribute('data-delay') || '0';
          setTimeout(() => {
            entry.target.classList.add('revealed');
          }, parseInt(delay));
          observer.unobserve(entry.target);
        }
      });
    }, observerOptions);

    document.querySelectorAll('.reveal-on-scroll').forEach(el => {
      observer.observe(el);
    });
  });
</script>
